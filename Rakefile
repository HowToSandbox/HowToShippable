require 'rake/testtask'

task :default => [:HelloWorld]

    Rake::TestTask.new do |t|
      #t.libs = ["support"]
      t.name = :HelloWorld
      t.warning = false
      t.verbose = false
      puts "Hello World"
    end


